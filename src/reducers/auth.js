const INITIAL_STATE = {
  currentUser: {
    email: 'genius',
    token: ''
  }
};

export default function (state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state
  }
}
